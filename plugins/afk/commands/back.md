---
description: Back at your desk. Normal permissions.
---

# Back

Disable AFK mode. Tool requests will use normal Claude Code permissions.

Execute the back script to toggle AFK mode off:

```bash
python3 ${CLAUDE_PLUGIN_ROOT}/scripts/cmd_back.py
```

The script will confirm when AFK mode is disabled. If already back, it will let you know.
